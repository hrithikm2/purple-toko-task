import 'package:flutter/material.dart';

///All the constant colors of the application will be used from here.
class AppColors {
  static const Color primaryColor = Color(0xffffffff);
  static const Color secondaryColor = Color(0xff81057d);
}
